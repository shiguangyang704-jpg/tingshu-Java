<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.atguigu.tingshu.order.mapper.OrderInfoMapper">

    <!--
        单步查询数据：
    -->
    <resultMap id="OrderInfoMap" type="com.atguigu.tingshu.model.order.OrderInfo">
        <id property="id" column="id"/>
        <collection property="orderDetailList" ofType="com.atguigu.tingshu.model.order.OrderDetail"
            select="selectDetail" column="id">
        </collection>
    </resultMap>
    <!--
       订单的字段
   -->
    <sql id="orderInfoSql">
        oi.id,
       oi.user_id,
       oi.order_title,
       oi.order_no,
       oi.order_status,
       oi.original_amount,
       oi.derate_amount,
       oi.order_amount,
       oi.item_type,
       oi.pay_way
    </sql>
    <!--
        订单明细字段
    -->
    <sql id="orderDetailSql">
        od.id ,
               od.item_id,
               od.item_name,
               od.item_url,
               od.item_price
    </sql>
    <!--
        根据订单状态查看我的订单！
    -->
    <select id="selectUserPage" resultMap="OrderInfoMap">
        select
            <include refid="orderInfoSql"></include>
        from order_info oi
        <where>
            oi.user_id = #{userId}
            <if test="orderStatus != null and orderStatus != ''">
                and oi.order_status = #{orderStatus}
            </if>
            and oi.is_deleted = 0
        </where>
    </select>

    <!--
        查看订单明细
    -->
    <select id="selectDetail" resultType="com.atguigu.tingshu.model.order.OrderDetail">
        select
            <include refid="orderDetailSql"></include>
        from order_detail od
        where od.order_id = #{id}
          and od.is_deleted = 0
    </select>
</mapper>

